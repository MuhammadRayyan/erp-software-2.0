import Link from "next/link";
import { ArrowLeft } from "lucide-react";
import { requireModule } from "@/core/permissions/require-module";
import { getExpenseAccountOptions } from "@/modules/accounting/services/account-service";
import { getActiveTaxCodes } from "@/modules/accounting/services/tax-code-service";
import { PurchaseOrderForm } from "@/modules/purchase-orders/purchase-order-form";
import { listActiveSuppliers } from "@/modules/suppliers/supplier-service";
import { listProjectOptions } from "@/modules/projects/project-service";
import { listInventoryItemOptions } from "@/modules/inventory/inventory-item-service";
import { getCurrencySettings } from "@/modules/currency/exchange-rate";

export default async function NewPurchaseOrderPage({ params, searchParams }: { params: Promise<{ businessId: string }>; searchParams: Promise<{ supplierId?: string; projectId?: string }> }) { const { businessId } = await params; const query = await searchParams; const { user, access } = await requireModule(businessId, "purchases"); const suppliers = listActiveSuppliers(businessId, user.id); const accounts = getExpenseAccountOptions(businessId, user.id); const taxes = getActiveTaxCodes(businessId, user.id).filter((tax) => ["purchases", "both"].includes(tax.direction) && tax.vatCategory); const projects = listProjectOptions(businessId, user.id); const items = listInventoryItemOptions(businessId, user.id); const currencySettings = getCurrencySettings(businessId, user.id); const today = new Date().toISOString().slice(0, 10); const defaultTax = taxes.find((tax) => tax.rateBasisPoints === 500 && tax.vatCategory === "standard")?.id ?? taxes[0]?.id ?? ""; const projectId = projects.some((project) => project.id === query.projectId) ? query.projectId! : ""; const supplierId = suppliers.some((supplier) => supplier.id === query.supplierId) ? query.supplierId! : ""; const documentCurrency = suppliers.find((supplier) => supplier.id === supplierId)?.defaultCurrencyCode ?? access.business.currency; return <div className="page-container page-wide"><Link href={`/b/${businessId}/purchases/orders`} className="mb-5 inline-flex items-center gap-1.5 text-[13px] text-muted-foreground hover:text-foreground"><ArrowLeft className="size-4" /> Purchase Orders</Link><div className="mb-7"><h1 className="page-title">New Purchase Order</h1><p className="page-description">Save a draft or issue an operational order without ledger impact.</p></div><PurchaseOrderForm businessId={businessId} suppliers={suppliers.map(({ id, name, defaultCurrencyCode }) => ({ id, name, defaultCurrencyCode }))} expenseAccounts={accounts.map(({ id, code, name }) => ({ id, code, name }))} taxCodes={taxes.map(({ id, name, rateBasisPoints }) => ({ id, name, rateBasisPoints }))} projects={projects} items={items.map(({ id, sku, name, purchasePriceMinor, inventoryAssetAccountId }) => ({ id, sku, name, purchasePriceMinor, inventoryAssetAccountId }))} currency={access.business.currency} currencies={currencySettings.currencies.filter((currency) => currency.is_active).map((currency) => ({ code: currency.code, name: currency.name, minorUnit: currency.minor_unit }))} rates={currencySettings.rates.map((rate) => ({ id: rate.id, currencyCode: rate.currency_code, rateDate: rate.rate_date, rateToBase: rate.rate_to_base, source: rate.source, sourceReference: rate.source_reference }))} initial={{ currencyCode: documentCurrency, exchangeRateToBase: documentCurrency === access.business.currency ? "1" : "", exchangeRateDate: documentCurrency === access.business.currency ? today : "", exchangeRateSource: documentCurrency === access.business.currency ? "Base" : "", supplierId, projectId, date: today, expectedDate: "", reference: "", notes: "", lines: [{ itemId: "", description: "", quantity: "1", unitPrice: "0.00", expenseAccountId: "", taxCodeId: defaultTax, projectId: "" }] }} /></div>; }
